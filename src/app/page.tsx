'use client'

import { HeroCard } from '@/components/landing/HeroCard'

export default function LandingPage() {
  return (
    <main className="noise min-h-screen flex items-center justify-center bg-gradient-to-b from-neutral-900 via-neutral-950 to-black">
      <HeroCard />
    </main>
  )
}
